# Set these yourself. Locally, copy this file to .env.local.
# When deployed, set the same names in the Vercel project environment variables.
# Do not hardcode keys. Do not put them in frontend code. This MVP has no Settings screen.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
HUBSPOT_ACCESS_TOKEN=
